import { makeStyles } from '@material-ui/core';

export default makeStyles({
  notFound: {
    width: '100%',
    height: '100%',
    padding: '1rem',
    display: 'flex',
    alignitems: 'center',
    justifyContent: 'center',
  
    'h1': {
      margin: '0 0 100px',
    },
  }  
});

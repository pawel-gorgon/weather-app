import { makeStyles } from '@material-ui/core';

export default makeStyles({
  container: {
    minHeight: '100vh',
    backgroundImage: 'url(/weather-app/imgs/background.png)',
    backgroundSize: 'cover',
  },

  content: {
    padding: '0 0 1px',
    position: 'relative',
  },
});
